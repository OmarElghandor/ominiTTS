{
  "$schema": "https://railway.com/railway.schema.json",
  "$comment": "Set RAILWAY_RUN_UID=0 in the Railway dashboard for omnivoice-api — volumes mount as root and the entrypoint must chown MODEL_STORE_DIR before dropping to appuser.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
